#include "main.h"
#include <stdio.h>
char *_strcat(char *dest, char *src);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	int i,j;
	for (i = 0 ;dest[i] ; i++ );
	for (j = 0; src [j] ; i++ );
	{
		dest[i] = src[j];
	}
	dest[i]='\0';
	return(dest);
}
